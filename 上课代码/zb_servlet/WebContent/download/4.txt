刘志行
liuzx@briup.com


javaEE第三阶段

html/css/servlet/jsp  8

mybatis框架  4

-----项目

spring+spring MVC 11

Web技术的认识
SSM

javaEE项目：
1个功能
前台页面--后台逻辑--数据库
						|
				 --
三层架构，MVC....
三层架构：
登录:username,password

public class Login{
		1000行

		100：与用户交互
		500：业务逻辑
		400：数据库操作
}

WEB：表示层[与用户交互，封装数据,【处理数据】处理好的数据渲染页面，跳转页面]
	展示数据、内容:html/css
	获取用户传送的数据:servlet    / springMVC

	返回数据到页面：servlet+jsp

		username,password

		name,pwd,eamil,phone,address....User userObj
		UserService usObj = new UserService();
		usObj.register(userObj);



SERVICE：业务逻辑[处理业务逻辑]:java
		1.判断用户名是否存在
			数据库操作daoObj
			daoObj.findUserByName(username);
			2.1如果存在，判断密码是否正确
				  3.1如果密码正确，登录成功，进入首页
 				  3.2密码错误，调回登录页面，提示信息
 			2.2如果不存在，跳回登录页面，提示信息

 		1.判断用户名是否存在
 			数据库操作dao
 			UserDao daoObj = new UserDao();
 			daoObj.findUserByName(userObj.getUserName());
 		  2.1如果存在，保存入库，跳转到登录
 		  		daoObj.saveUser(userObject);
 		  2.2如果不存在，转回注册页面，提示信息

DAO：数据访问层(增删改查):JDBC,mybatis
	findUserByName(
		Sql="select * from user where name = ?"
	)
	saveUser{
		Sql="insert into user values(?,?,?,?)"
	}
	upDateUser
	findUserById
	findUserByClazz

JAVA EE


2.HTML+CSS
	html是什么，做什么，怎么做
	html（超文本标记mark语言）
	网页
	【代码:标签】

	标记语言
	XML：自由设定，标签可以自己定义
	<note>
		<from>zs</from>
		<to>ls</to>
	</note>

	html:标签都是预定义的,html的标签被W3C定义

	块级：独占一行
	行内：可以和其他行内元素同处一行



























































